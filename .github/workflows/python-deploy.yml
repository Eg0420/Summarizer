name: Python Service Deploy

on:
  push:
    branches: [ main ]
    paths:
      - 'pythonservice/**'
      - '.github/workflows/python-deploy.yml'

jobs:
  deploy-pythonservice:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.11'

    - name: Install dependencies
      working-directory: ./pythonservice
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt

    - name: Run tests
      working-directory: ./pythonservice
      run: python -m pytest --tb=short

    - name: Build Docker image
      working-directory: ./pythonservice
      run: |
        docker build -t summarizer-pythonservice:${{ github.sha }} .
        docker tag summarizer-pythonservice:${{ github.sha }} summarizer-pythonservice:latest

    - name: Deploy to Railway (placeholder)
      run: |
        echo "Python service Docker image built successfully"
        echo "Ready for deployment to Railway, Render, or similar platform"
        echo "Image: summarizer-pythonservice:${{ github.sha }}"

    - name: Upload Docker image artifact
      uses: actions/upload-artifact@v4
      with:
        name: pythonservice-image
        path: |
          pythonservice/
          !pythonservice/test_data/
          !pythonservice/data/
        retention-days: 7